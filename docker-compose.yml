version: '3.8'

services:
  sqlcoder-app:
    build: .
    ports:
      - "8000:8000"
      - "8501:8501"
    volumes:
      - ./database:/app/database
    environment:
      - PYTHONPATH=/app
    restart: unless-stopped

  # Optional: Add PostgreSQL for production
  # postgres:
  #   image: postgres:15
  #   environment:
  #     POSTGRES_DB: sqlcoder_db
  #     POSTGRES_USER: sqlcoder
  #     POSTGRES_PASSWORD: password
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   ports:
  #     - "5432:5432"

# volumes:
#   postgres_data:
